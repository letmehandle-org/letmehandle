name: Mobile build

# Building the native applications costs minutes a backend change has no reason to spend, so
# this runs only when something under the mobile app actually changes.
on:
  pull_request:
    paths:
      - 'apps/mobile/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/actions/pnpm-install/**'
      - '.github/workflows/mobile-build.yml'
  push:
    branches: [main]
    paths:
      - 'apps/mobile/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  ios:
    name: iOS
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7

      - uses: ./.github/actions/pnpm-install

      # 3.4 rather than 3.3: Gemfile.lock is written by a current bundler, and bundler 4
      # requires a Ruby newer than 3.3. The alternative is pinning an old bundler in a
      # generated file by hand, which is how the previous attempt failed — macOS's own Ruby
      # 2.6 wrote "BUNDLED WITH 1.17.2", and bundler 1 calls String#untaint, removed in
      # Ruby 3.2.
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: true
          working-directory: apps/mobile

      - uses: actions/cache@v4
        with:
          path: apps/mobile/ios/Pods
          key: pods-${{ runner.os }}-${{ hashFiles('apps/mobile/ios/Podfile.lock', 'pnpm-lock.yaml') }}

      - name: Pods
        working-directory: apps/mobile/ios
        run: bundle exec pod install

      - name: Build
        working-directory: apps/mobile/ios
        run: |
          xcodebuild \
            -workspace LetMeHandle.xcworkspace \
            -scheme LetMeHandle \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            build

  android:
    name: Android
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v7

      - uses: ./.github/actions/pnpm-install

      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v6

      - name: Unit tests
        working-directory: apps/mobile/android
        run: ./gradlew :app:testDebugUnitTest --no-daemon

      - name: Build
        working-directory: apps/mobile/android
        run: ./gradlew assembleDebug --no-daemon
