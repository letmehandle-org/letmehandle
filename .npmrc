# React Native's Metro bundler and the iOS/Android build systems resolve modules by walking
# node_modules, and neither understands pnpm's symlinked store. Hoisting is not a preference
# here; without it the native builds do not find their dependencies.
node-linker=hoisted
strict-peer-dependencies=false
