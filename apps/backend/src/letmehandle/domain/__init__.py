"""The product, expressed without reference to anything outside it.

Nothing here may import a framework, a driver, or a vendor SDK. That is enforced by the
import-linter contracts in pyproject.toml, not by convention.
"""
