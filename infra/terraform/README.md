# terraform

Unused.

There is no deployment target yet, and infrastructure code for a deployment that does not
exist is code that is wrong by the time it does. Local development runs on
`docker-compose.yml` at the repository root.

This directory exists so that the answer to "where would it go" is already settled.
